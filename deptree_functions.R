library(infotheo)
library(tidyverse)
library(optrees)
library(igraph)

deptree_known_root <- function(value_df, random_effects_df, root){
    value_df_for_depfun <- as.data.frame(lapply(value_df, as.factor))
  
  
}
  
  

